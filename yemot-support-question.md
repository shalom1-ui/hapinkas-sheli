# פנייה לתמיכה הטכנית של ימות המשיח - בעיה בשמירת הקלטה במצב "record" דרך שלוחת API

שלום רב,

יש לי שלוחה מסוג **מודול API** (type=api) שמחוברת לשרת חיצוני שלי (api_link, api_url_post=yes).
במהלך שיחה אני שולח לימות פקודת `read=` המבקשת מהמתקשר **להקליט** את קולו (מצב "record" בפרוטוקול ה-read), כדי שאני אוכל להוריד את ההקלטה בעצמי אחר כך דרך `DownloadFile` (אני לא צריך את מנוע זיהוי הדיבור המובנה בשלב הזה - רק את ההקלטה הגולמית).

**הבעיה: ההקלטה אף פעם לא נשמרת בפועל בשלוחה**, למרות שהשיחה עצמה מתנהלת תקין (המתקשר מדבר, שומע את תפריט האישור, מקיש כדי לאשר) - ובדיקה מול `GetIVR2Dir` על אותה שלוחה מראה תמיד `"files":[]` (רשימה ריקה לגמרי), גם מיד אחרי שיחת בדיקה.

## פרטים טכניים

- **מספר שלוחה**: 1 (מוגדר `extType:"api"` דרך `ext.ini`)
- **פקודת ה-read שאני שולח** (הפורמט המדויק, עם ערכים לדוגמה):
  ```
  read=t-<טקסט לדוגמה>=speech,no,record,1,hp-<מזהה-שיחה>,yes,yes,,,15
  ```
  כלומר, לפי סדר הפרמטרים המוכר לי: `valName=speech`, `re_enter_if_exists=no`, מצב=`record`, `path=1`, `file_name=hp-<מזהה-שיחה>`, `no_confirm_menu=yes`, `save_on_hangup=yes`, `append_to_existing_file=` (ריק), `min_length=` (ריק), `max_length=15`.
- **מה שקורה בפועל בשיחה**: אחרי שהמתקשר מדבר, נשמעת אצלו הודעת המערכת (זיהיתי אותה דרך `GetIVR2Dir` בשדה `msgDescriptions`, מזהה הודעה `M1553`):
  > "לאישור ההקלטה הקישו 1, לשמיעה הקישו 2, להקלטה מחדש 3"

  המתקשר מקיש **1** (לאישור), ולפעמים גם בודק ומקיש **2** (לשמיעה) ושומע את ההקלטה חוזרת אליו כרגיל - כלומר ההקלטה עצמה בהחלט **נקלטת** בזמן אמת אצל ימות.
- **הבעיה**: בכל זאת, בדיקה מיד אחרי השיחה מול:
  ```
  GetIVR2Dir?token=...&path=1
  ```
  מחזירה `"dirs":[],"files":[]` - כלומר שום קובץ הקלטה לא נשמר בתיקיית השלוחה בסופו של דבר, אף פעם.
- כתוצאה מכך, קריאה ל-`DownloadFile?token=...&path=ivr2:1/hp-<מזהה-שיחה>.wav` **תמיד** מחזירה 404 (קובץ לא קיים).

## מה כבר בדקתי וניסיתי (בלי הצלחה)

1. ודאתי שמספר השלוחה נכון (1) - לא מספר הטלפון/DID של הקו.
2. ודאתי שאין הגבלות כלשהן (IP / Service / Parameter Restrictions) על מפתח ה-API בשימוש - כולן ריקות.
3. שיניתי את `no_confirm_menu` בין `"yes"` ל-`"no"` - **אין שום הבדל בהתנהגות** (תפריט האישור תמיד מושמע בכל מקרה).
4. הנחיתי את המתקשר להקיש `#` (סולמית) בסיום הדיבור (לפי `record_ok=#` שמתועד עבור סוג שלוחה אחר, לא-API) - **לא עזר**.
5. הנחיתי את המתקשר להקיש `1` (בדיוק לפי טקסט תפריט האישור בפועל, M1553) - **גם לא עזר**, ההקלטה עדיין לא נשמרת.
6. ניסיתי לשלוח את פרמטר ה-`path` בפקודת ה-record גם בפורמט עם קידומת `ivr2:` (כמו ש-`DownloadFile` דורש), במקום המספר הגולמי (כמו ש-`GetIVR2Dir` מקבל) - **גם לא עזר**.

## נספח - דוגמה אמיתית מלוג שיחת בדיקה אחרונה

הרצתי שיחת בדיקה אמיתית (ApiCallId: `78b9a09302058db6607a7957c39e5034432bfc07`) ותיעדתי בדיוק את מה שנשלח וחזר:

1. פנייה ראשונה של ימות אליי (בקשת הקלט הבאה בשיחה) - עדיין לפני שהמתקשר דיבר.
2. אחרי שהמתקשר דיבר והקיש 1 לאישור (לפי הודעת המערכת M1553, ר' למטה), ימות שלחה אליי בחזרה:
   ```
   "speech":"ivr2:1/hp-78b9a09302058db6607a7957c39e5034432bfc07.wav"
   ```
   (כלומר היא "הבינה" ומחזירה בדיוק את הנתיב שביקשתי לשמור אליו - סימן שהפקודה עצמה כן פוענחה נכון).
3. מיד ניסיתי להוריד את הקובץ מאותו נתיב בדיוק (`DownloadFile?path=ivr2:1/hp-....wav`) - **תוצאה: 404**, בשני ניסיונות (עם השהיה של 1.5 שניות ביניהם).
4. בדיקת `GetIVR2Dir?path=1` **מיד אחרי** אותה שיחה מחזירה `"dirs":[],"files":[]` - ריק לגמרי. שווה לציין ש-`ext.ini` עצמו כן מופיע שם (`"ini":[{"exists":true,"name":"ext.ini",...}]`) - כלומר הבדיקה מול השלוחה הנכונה, זה לא עניין של path שגוי לגמרי.

**הודעות המערכת הרלוונטיות שימות מחזירה עבור שלוחה זו** (ציטוט מדויק משדה `msgDescriptions` בתשובת `GetIVR2Dir`):
- `M1553`: "לאישור ההקלטה הקישו 1 / לשמיעה הקישו 2 / להקלטה מחדש 3" - זו ההודעה שנשמעת בפועל למתקשר אחרי ההקלטה במקרה שלנו.
- לשם השוואה, קיימות גם הודעות מערכת אחרות שקשורות ל"הקלטת שם" (`M1108`: "אנא אמרו את שמכם ושם משפחתכם לאחר הצליל", `M1109`: "אם אתם מרוצים מההקלטה הקישו 1, להקלטה מחודשת 2", `M1110`: "הקלטת השם נקלטה בהצלחה") - יכול להיות שאלה שייכות לפיצ'ר מובנה ומתועד יותר של "הקלטת שם למחייג" שאינו קשור למה שאני מנסה לעשות (הקלטה חופשית לצורך תמלול חיצוני) - אשמח לדעת אם יש הבדל משמעותי בהתנהגות בין השניים.

## השאלה שלי

מה הפורמט/הפרמטרים המדויקים הנדרשים כדי שהקלטה במצב "record" (בתוך פקודת `read=`) **תישמר בפועל** לשלוחה כשהיא מופעלת דרך **שלוחת מודול API** (type=api, לא שלוחת "הקלטת הודעה" רגילה)? האם יש הבדל התנהגות ידוע בין הפעלת מצב record דרך שלוחת API לבין שלוחות אחרות? האם חסר לי פרמטר נוסף שלא מופיע בתיעוד הכללי (הלא-ספציפי ל-API)?

תודה מראש על העזרה!
